# Course skill router (Ohm's Law / DC)

Cognitive job first. Then the skill. Then one HTML lesson. Do not clone `01-resistance.html` as a whole page. That is what made Voltage, Current, and Ohm look like the same lesson with the title swapped.

This file wires the agent. Read it at the start of every DC lesson chat, before you open HTML.

Identity (shared): `ELECTRIC-INK-LOCK.md` for page chrome **and** 16:9 teaching stages. Dark ink, Space Grotesk / IBM Plex, `#ldock`, quantity colors (violet V, cyan I, amber R). Not NEETS paper. Not Newsreader / Hanken / `#f4f0e7`.

Cards: steal the **job** from `CardCraft-Library/references/slide-types-catalog.md`. Restyle the 16:9 stage to Electric Ink tokens (`.cc-card` / `.cc-stage`). The gold paper file `aero-slide-types-beyond-video.html` is the public extract's AERO register, not this lesson's skin. Do not charcoal-skin CardCraft JS and call it done. Sequence It (09) is order with a consequence. If the shop can set the meter before killing power, do not force a 1-2-3. That is a sort (anytime / must / never), not a sequence.

Do not load Genesis `ACE-html-training-factory-audit.md`. It is superseded. It mixed PEOPLE / ACE player with this folder.

Voice (shared): `NICK-VOICE-STYLE-GUIDE.md` then `NICK-AET-CERT-BOOK.md` then `nick-aet-voice`.

Centerpiece (unique per lesson): pick from the tables below. If two lessons would use the same poke, one of them is wrong.

---

## 0. Load order for one lesson chat

1. Signed textbook chapter for this day (`NN-slug-chapter.md`, or the matching chapter in `CAET Textbook.html`). If missing, stop and run `caet-player-editor`. Do not open HTML yet.
2. This router (DC C-numbers).
3. `NICK-VOICE-STYLE-GUIDE.md` (sentences).
4. `NICK-AET-CERT-BOOK.md` (definitions and the 9 V walk).
5. `CAET-LESSON-SEQUENCE.md` for this C-number only (topics and LO **codes**, not exam stems).
6. `PRACTICE-QUESTION-BANK.md` for the checkpoint. Never open the GMetrix workbook to write a question.
7. The skill for the **centerpiece** of this lesson (see table in section 2).
8. `electric-ink-id` plus `chassis.md` for frozen dock/gates/Continue.
9. House rules: `CLAUDE.md` (em-dashes, single file, contrast, notes, notify-audit).

Do not load every skill in the workspace. Load the ones this beat needs. Extra skill files tax the context window and make the lesson generic.

Clone stencil: `electric-ink-chassis/lesson-chassis.html` after a signed chapter. The wrap sheet (`NN-slug-wrap.md`) names the cards. The chapter prose goes on the page first. Live DC proof (engine, not a stencil): `AERO-Player/courses/caet-dc/lessons/01-resistance.html`. Do not overwrite that player file with the ungated author HTML in this folder. This router is the DC C-number centerpiece table. Non-DC CAET days still clone the stencil and drop the cards named on the wrap sheet. They do not steal the Resistance beat list.

---

## 1. Decide the beat, then the skill

Ask: what must the student **do** on this beat? Then load only that row.

| Student job | Load | Do not load |
|---|---|---|
| Write like Nick (definitions, formulas, meter lists, walked number) | `nick-aet-voice` + `NICK-VOICE-STYLE-GUIDE.md` | `storybrand-framework`, `copywriting-formulas` |
| Plan the lesson before HTML | `training-architect` DESIGN mode, then stop for Nick's yes | Building HTML in the same breath |
| Shared Electric Ink chrome (dock, type, quantity color, checkpoint shell) | `electric-ink-builder` **tokens and dock only** | Copying Resistance's lattice, color decoder, or rho bench |
| One poke: predict, flip a part photo, add/remove a network part, virtual meter, scored check | `electric-ink-id` | A minigame on every panel |
| See current, drops, or Ohm's Law **computed on a live schematic** | `circuit-simulator-builder` (read its references, then build **this** topology) | Pasting the locked ohms-lab template unchanged |
| See a waveform, time/div, peak, RMS | `oscilloscope-viewer-builder` | Fake scope SVG with no divisions |
| Motion **is** the teaching (Kirchhoff adding, conventional vs electron, water-tank voltage) | `/hyperframes` then `/hyperframes-core` and `/hyperframes-animation`; render with `aero-slide-video-builder` + `aero-video-studio/_builder-kit.md` | HTML boxes that count numbers and calling it HyperFrames |
| Short unnarrated sting (under ~10 s, motion is the message) | `/motion-graphics` | A 14 s title card with no teaching motion |
| Host a finished sim inside the AERO player later | `simulation-beat-builder` | Doing this before the sim exists |
| Hotspot on a real photo or drawing, clickable block diagram | `art-library/patterns/interaction-patterns.md` (Sample 10 hotspot, Sample 11 block diagram) | Inventing an aircraft-path cartoon SVG |
| Discriminate two **identical** scenes except the defect (good vs cold joint) | CardCraft catalog 10 from `aero-slide-types-beyond-video.html` (clip-path wipe, AERO register stage) | A volts-vs-amps slider. Two different hookups. `input type=range` that resizes artwork |
| Magazine-depth visual craft for a beat (jobs, blocks, restraint) | `CardCraft-Library/skills/visual-learning-builder` **block-library + craft-principles only** | Putting "CardCraft" on a kicker; using `cardcraft-builder` ACE player acts as this lesson's shell |
| Branching "what do you do" judgment | `narrative-training-module-builder` | DC formula lessons |
| Narrated multi-lesson HF / procedures | `aero-course-player-builder` | This Ohm's Law HTML pass |
| Palette / type pass on a player course | `aero-course-stylist` | Restyling Electric Ink to paper |
| QA a player course | `aero-course-inspector` | Skipping render-and-look |
| SCORM zip | `aero-export-packager` | PowerShell Compress-Archive |
| Scored check / practice items | `PRACTICE-QUESTION-BANK.md` (pull the live set) | The GMetrix / USI workbook Question-LO Map |
| No intake yet | `project-intake` then `reference-builder` | Guessing LOs |

CardCraft `cardcraft-builder` builds ACE Course Player **acts**. Wrong vehicle for these DC HTML files unless Nick names that product. Steal craft from its block library. Do not steal its player shell.

---

## 2. This course: one unique centerpiece each

If you cannot name the centerpiece in one line, you are about to clone Resistance.

| ID | File | Centerpiece (unique) | Skills for that centerpiece |
|---|---|---|---|
| C04 | `04-voltage.html` | Kirchhoff: drops add to the source, **while they read**. IEC cells in a broken wire. Voltmeter across a live part. | HyperFrames stack for the law. `electric-ink-id` for the meter poke. Book numbered voltmeter steps in prose. Photo cards or Sample-10 hotspots for battery / bus / generator. Not a lattice. Not a color decoder. |
| C05 | `05-current.html` | Flow: electron vs conventional, series I stays, parallel I adds, ammeter **in series**. | Particles or HyperFrames for direction. `electric-ink-id` networks + ammeter lab. Not voltage bars. Not color code. |
| C06 | Player: `AERO-Player/courses/caet-dc/lessons/01-resistance.html` (gold chassis). Author draft: `01-resistance.html` in this folder. | Opposition: types, series/parallel pull-a-part, **color code**, ohms with power OFF. Lattice belongs **here** if it stays. | `electric-ink-id` types + color + ohms. Chassis is locked. Improve blocks inside beats. Not a stencil for C04-C08. |
| C07 | `07-ohms-law.html` | The relationship: E = I * R, rearrange, walk 9 V / 65 ohm, Direct and Indirect. | `circuit-simulator-builder` for a live E-I-R object **or** a HyperFrames relationship clip, not both stacked. History is a portrait and a short bio, not a timeline widget. |
| C08 | `08-multimeter.html` | The tool: volts vs amps vs ohms hookup, fuse, polarity. | Sequence It for the job order. Explorable for the jacks. Numbered lists from the book. Side-by-side labeled diagrams for parallel vs series, not a wipe. Virtual meter from `electric-ink-id`. Not another Ohm sandbox. |

C09-C11 wait until Nick names them.

---

## 3. Shared vs stolen

Steal these from any finished DC file:

- CSS variables, type stacks, `#ldock` behavior, checkpoint **state machine**, validate.py floor, `-notes.md` shape, `notify-audit.ps1`.

Do not steal these from Resistance into another lesson:

- Rho L / A bench, drag lattice, 4-band decoder, Resistance hero LOs, Resistance map-card copy.

Each lesson keeps: one aircraft or bench ticket that earns the idea, one live object, one shop skill, one scored check. The **form** of those four is chosen from section 1, not copied.

Stay put. Gate once. Steal CardCraft **jobs** in Electric Ink (`.cc-card`). Do not clone Resistance's 11 beats, lattice, color decoder, or rho bench. Voltage is C04: 9 V divider while they read, voltmeter power ON in parallel, flip battery / bus / generator. Plan the beat map. Do not write HTML until Nick signs it.

---

## 4. HyperFrames (do not fake it)

If motion teaches:

1. Read `/hyperframes` (install with `npx skills add heygen-com/hyperframes --all` if core/animation are missing).
2. Read `/hyperframes-core` and `/hyperframes-animation`.
3. Read `aero-slide-video-builder` and `aero-video-studio/_builder-kit.md`.
4. Clone the **head and timeline contract** from `aero-video-studio/index.html` (fonts, paused `window.__timelines["main"]`, onUpdate from `tl.time()`). Not the reactance story.
5. Render, extract frames, LOOK at the PNGs, fix, re-render.
6. Embed muted loop + controls beside the prose they must read. Pause when `prefers-reduced-motion`.

A count-up that shows a wrong sum mid-clip is not Kirchhoff.

---

## 5. Voice vs chrome

`nick-aet-voice` writes sentences. It does not pick the interaction. After copy is in his spine, come back to this router for the poke.

NEETS-like means walked numbers, numbered tools, faults as readings. It does not mean paper, Fraunces, or a left rail.

---

## 6. Assembly later (not this pass)

These HTML files are the authoring masters. Player vs Thinkific is Nick's call. When he says assemble: `aero-course-player-builder` then `simulation-beat-builder` for labs then `aero-export-packager`. Do not pick a third vehicle.

---

## 7. Done for a lesson

- Centerpiece is unique. A student who took Resistance would not think they opened Resistance again.
- Copy passes `NICK-VOICE-STYLE-GUIDE.md`.
- Checkpoint items came from `PRACTICE-QUESTION-BANK.md`. You did not open the certification workbook for wording.
- Skills you loaded are named in the companion `-notes.md`.
- `python tools/validate.py --dir "Training Projects/DC Fundamentals AI Rebuild"` : 0 FAILs on this HTML.
- Render and look. `notify-audit.ps1` when Nick should look.
