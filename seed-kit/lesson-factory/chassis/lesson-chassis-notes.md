# lesson-chassis.html notes

Template for Electric Ink CAET lessons in AERO Player. Built 2026-08-28. Spine rewritten the same day so the poke is a recipe, not a DC clone.

## What this is

A clone stencil. Spine: Start (read), Why (read, optional), one Poke (lab, pick a recipe), Check, Field card. Dummy copy. Real `__inkGate`, dock ticks, `#ldHint`, Continue.

This file's stand-in poke is the **decide** recipe (skipped written turnover, which Dirty Dozen factor). Do not keep decide on every clone.

Skills: `electric-ink-id` (`chassis.md` + `beat-recipes.md`), `electric-ink-builder` (steal list for DC C-numbers). Voice placeholders, not Nick's book lines.

Plans for the 45 days: `CAET 9 Week Program/CAET Daily Lesson Plans.html`.

## What this is not

Not Resistance. Not gold teaching. Not the apprenticeship engineering-drawing shell. Not a third player. Not the sellable HF catalog skin (`complacency.html`).

## Clone contract

Search `SWAP_`. Pick the poke from `beat-recipes.md`. Checkpoint ITEMS from `PRACTICE-QUESTION-BANK.md` on DC days. LOs from `caet-lo-registry.md` or that day's plan. Drop Why if it does not teach. Field card plates = this day's LO count.

## Verify

Drive: Begin, Why Continue, Call Continue stays dead until a factor is picked, check fail then 2 of 2, Mark complete enables. Locked dock ticks flash the hint.
